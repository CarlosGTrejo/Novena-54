{
  "name": "novena-54",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "git add dist && git stash -- dist && git checkout dist && git stash pop && git mv -f dist docs && git add -- docs && git commit -m 'update' && git push"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^1.1.0",
    "svelte": "^3.52.0",
    "vite": "^3.2.3"
  }
}
